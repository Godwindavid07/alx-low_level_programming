#include <stdio.h>

/**
 * main - prints the alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'Z'; i++)
	{
		putchar(i);
	}
			putchar('\n');
			return (0);
}
